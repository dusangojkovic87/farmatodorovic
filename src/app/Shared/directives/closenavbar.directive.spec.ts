import { ClosenavbarDirective } from './closenavbar.directive';

describe('ClosenavbarDirective', () => {
  it('should create an instance', () => {
    const directive = new ClosenavbarDirective();
    expect(directive).toBeTruthy();
  });
});
